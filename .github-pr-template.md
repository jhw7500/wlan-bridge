## 개요 (Summary)

L2 브리지의 패킷 필터링 로직을 개선하여 코드 명확성을 높이고 향후 확장성을 확보합니다.

- ✅ Multicast/Broadcast 명시적 처리
- ✅ ARP 필터링 추가
- ✅ IP 필터 간소화 (MAC 체크 제거)

## 문제점 (Problem)

### 1. Multicast/Broadcast 처리 불명확
- L2 브리지 원칙에 따르면 broadcast/multicast는 무조건 포워딩되어야 함
- 현재 코드는 명시적 처리가 없어 향후 수정 시 broadcast 차단 위험 존재

### 2. ARP 트래픽 누수
- 브리지 IP로 향하는 ARP Request도 eth0로 포워딩됨
- OPC가 불필요한 ARP 패킷을 수신 (미미하지만 최적화 가능)

### 3. IP 필터의 MAC 검증
- IP+MAC 동시 체크로 인한 불필요한 제약
- MAC 스푸핑 환경에서는 IP만으로 충분

## 해결 방법 (Solution)

### 변경 사항
```c
// 1. Multicast flag 체크 추가
const int is_multicast = eth->h_dest[0] & 0x01;

// 2. ARP 필터링 함수 추가
static int is_arp_for_bridge(const struct pcap_pkthdr *hdr,
                              const uint8_t *payload,
                              size_t header_len)

// 3. IP 필터 간소화
static int is_packet_to_bridge_ip_only(uint32_t dst_ip)  // MAC 파라미터 제거
```

### 코드 흐름 개선
```
Before:
패킷 수신 → VLAN 파싱 → MAC 필터 → IP 필터 → 포워딩

After:
패킷 수신 → Multicast 체크 → VLAN 파싱
  → Multicast? → ARP 필터만 적용 → 포워딩
  → Unicast? → MAC/IP 필터 → 포워딩
```

## 주요 수정 내용 (Changes)

### 1. 헤더 추가 (dumb.c:24)
```c
#include <net/if_arp.h>  // ARPHRD_ETHER 정의
```

### 2. 함수 추가/수정 (dumb.c:289-343)
- `is_packet_to_bridge_ip_only()`: MAC 체크 제거, IP만 검증
- `is_arp_for_bridge()`: ARP Target IP가 브리지 IP인지 확인

### 3. ph() 함수 개선 (dumb.c:345-430)
- Multicast flag 체크 추가
- Multicast/Broadcast 우선 처리
- ARP 필터링 적용
- IPv4 Multicast 범위 체크 (224.0.0.0/4)
- 로그 빈도 제한 (1000번마다)

## 테스트 결과 (Testing)

### 기본 연결
- [x] RDP 연결 정상 (노트북 → OPC)
- [x] SSH 연결 정상 (노트북 → OPC)
- [x] 브리지 관리 SSH 정상
- [x] Ping 테스트 (0% loss, RTT < 5ms)

### 필터링 동작
- [x] Broadcast 정상 포워딩 (DHCP)
- [x] Multicast 정상 포워딩 (LLMNR)
- [x] 브리지 IP ARP가 eth0로 안 가는지 확인
- [x] 유니캐스트 IP 필터 정상 동작

### 성능
- [x] stats.dropped = 0
- [x] stats.pcap_drop = 0
- [x] CPU 사용률 변화 없음

## 영향 범위 (Impact)

### 기능 변경
- ✅ 기존 동작 유지 (RDP, SSH, DHCP 등)
- ✅ ARP 트래픽 최적화 (OPC로 안 감)
- ✅ Multicast 명시적 처리 (L2 원칙 준수)

### 코드 품질
- ✅ L2 브리지 의도 명확화
- ✅ 향후 IPv6/mDNS 등 확장 용이
- ✅ 주석 및 코드 가독성 향상

## 통계 (Statistics)

```diff
 dumb/dumb.c | 88 insertions(+), 17 deletions(-)
```

- 추가: 88 lines
- 삭제: 17 lines
- 순증가: 71 lines

## 체크리스트 (Checklist)

- [x] 코드 수정 완료
- [x] 커밋 메시지 작성
- [x] 기능 테스트 통과
- [ ] 현장 배포 테스트 (배포 전 필수)
- [ ] 문서 업데이트 (필요 시)

## 현장 환경 (Environment)

- Platform: i.MX8MM + NXP88W9098
- Kernel: Linux 5.4.0-169-generic
- 사용 옵션: `--ip-filter` (mac-filter 비활성화)
- 트래픽 유형: RDP, SSH, DNS, ARP

## 관련 문서 (References)

- L2 브리지 표준: IEEE 802.1D
- ARP RFC: RFC 826
- IPv4 Multicast: RFC 1112 (224.0.0.0/4)
