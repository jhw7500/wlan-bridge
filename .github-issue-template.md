## 배경 (Background)

현재 dumb 브리지는 `ip_filter` 옵션을 사용하여 브리지 관리 IP로 향하는 패킷을 필터링합니다.
그러나 다음과 같은 개선이 필요합니다:

### 현재 동작
- ✅ TCP/UDP 유니캐스트 트래픽: 정상 동작
- ✅ 브리지 관리 SSH/RDP: 정상 필터링
- ⚠️ Multicast 트래픽: 명시적 처리 없음 (LLMNR, mDNS 등)
- ⚠️ ARP 트래픽: 브리지 IP로 향하는 ARP도 유선으로 누수
- ⚠️ MAC 검증: IP+MAC 동시 체크로 인한 불필요한 제약

## 문제점 (Issues)

### 1. Multicast/Broadcast 처리 미흡
**현상**: Broadcast/Multicast 패킷이 일반 유니캐스트와 동일하게 처리됨

**영향**:
- L2 브리지 원칙 불명확 (향후 수정 시 broadcast 차단 위험)
- Windows LLMNR 등 멀티캐스트 트래픽이 불필요하게 유선으로 누수

**예시**:
```
Windows 노트북 → wlan0: LLMNR Query (224.0.0.252)
  → IP 필터: 224.0.0.252 != 브리지 IP
  → eth0로 포워딩 (OPC가 받지만 무시)
```

### 2. ARP 필터링 누락
**현상**: 브리지 IP로 향하는 ARP Request도 eth0로 포워딩됨

**영향**:
- OPC가 불필요한 ARP 패킷 수신 (미미하지만 최적화 가능)
- 유선 트래픽 증가

**예시**:
```
노트북 → wlan0: ARP "who has 192.168.1.100?"
  → ARP != IP이므로 필터 체크 안 됨
  → eth0로 포워딩 (불필요)
```

### 3. IP 필터의 MAC 검증
**현상**: `is_packet_to_bridge()`가 IP와 MAC을 동시 체크

**영향**:
- 현재 환경에서는 문제 없음 (IPv4 유니캐스트)
- IPv6 멀티캐스트 등 향후 확장 시 제약

**코드**:
```c
if (ifs.ipv4[k] != 0 &&
    ifs.ipv4[k] == dst_ip &&
    memcmp(dst_mac, ifs.mac[k], ETH_ALEN) == 0) {
    return 1;
}
```

## 제안 (Proposal)

### 1. Multicast/Broadcast 명시적 처리
```c
const int is_multicast = eth->h_dest[0] & 0x01;

if (is_multicast) {
    // ARP 필터링만 적용
    // 나머지는 무조건 포워딩 (L2 브리지 원칙)
    goto forward_packet;
}
```

### 2. ARP 필터링 추가
```c
if (cfg.enable_ip_filter && ethertype == ETH_P_ARP) {
    // ARP Target IP가 브리지 IP인지 체크
    if (is_arp_for_bridge(...)) {
        return; // 커널이 처리하도록
    }
}
```

### 3. IP 필터 간소화
```c
// MAC 체크 제거, IP만 체크
if (ifs.ipv4[k] != 0 && ifs.ipv4[k] == dst_ip) {
    return 1;
}
```

## 테스트 시나리오 (Test Scenarios)

### 기본 연결
- [ ] RDP 연결 정상 (노트북 → OPC)
- [ ] SSH 연결 정상 (노트북 → OPC)
- [ ] 브리지 관리 SSH 정상
- [ ] Ping 정상 (0% loss)

### ARP 필터링
- [ ] 브리지 IP ARP가 eth0로 안 가는지 확인
  ```bash
  # 노트북에서 ARP 캐시 삭제 후 ping
  arp -d 192.168.1.100
  ping -c 1 192.168.1.100

  # 브리지에서 eth0 tcpdump
  tcpdump -i eth0 arp
  # 출력: 브리지 IP ARP 없어야 함
  ```

### Multicast 포워딩
- [ ] Broadcast 정상 포워딩 (DHCP 등)
- [ ] Multicast 정상 포워딩 (Windows LLMNR 등)

### 성능
- [ ] stats.dropped = 0
- [ ] stats.pcap_drop = 0
- [ ] Ping RTT < 5ms

## 우선순위 (Priority)

**Medium** - 현재 기능적으로 문제는 없으나, 코드 명확성 및 향후 유지보수를 위해 권장

## 환경 (Environment)

- Platform: i.MX8MM + NXP88W9098
- Kernel: Linux 5.4.0-169-generic
- 사용 옵션: `--ip-filter` (mac-filter는 비활성화)
- 트래픽: RDP, SSH, DNS, ARP

## 참고 (References)

- L2 브리지 원칙: Broadcast/Multicast는 무조건 flooding
- MAC 스푸핑 환경: wlan0 MAC = 유선 장치 MAC
- ARP RFC: RFC 826
- IPv4 Multicast: RFC 1112 (224.0.0.0/4)
